import { JobsList } from '../../components/JobsList';

export default function ParentJobs() {
  return <JobsList title="My tutors" subtitle="Confirmed tutors, with full contact details unlocked." />;
}
